import StudentLetter from '/components/owls/StudentLetter.vue';

/* IMPORTANT! Component names (defined both in the exported objects
 * and below) must match the `impl` key of the target owl —
 * this is the foundation of dynamic owl view dispatch. */

export default {
  StudentLetter
};
